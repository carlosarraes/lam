# Private terminal observer checkpoint, 2026-09-20

The uninstalled debug candidate opened `lam chat --project <private-project>` in a transient human-owned terminal process, outside all supported agent process trees. It connected to the same private daemon and project as the three owned native sessions. No global hook, extension, Chat service, or cloud configuration was changed. The transient terminal service exited normally after `Tab`, then `q`.

The view showed the project's agent-to-agent feed and per-recipient Accepted/Unknown and exposure states without calling any participant Inbox or marking a message fetched. Typing `@owned-codex`, Enter, Tab, the body `HUMAN_TO_CODEX_TUI_920`, then Enter selected the exact pinned Codex session. The Store committed human-origin message `7c4cff67-2071-4deb-ac47-dc66958c8630`, the view displayed it, and Codex accepted native queue message `01a0c031-d576-7981-82ae-f5387ef33184` for exact thread `01a0c018-f492-7a42-bf1a-4a76ccc58852`. The thread transcript independently recorded the full LAM wrapper at 19:01:26.719 UTC. This is client acceptance, not model agreement or action.

A first transient launch failed closed on the user systemd manager's protected `/proc/.../exe`. The process-identity walk now stops only at an owned, parent-PID-1 `systemd --user` boundary after inspecting all closer processes. A second isolated human process could list the private roster, and the terminal view opened successfully. Recognized native agent ancestors still require their own exact participant binding; they do not fall back to human origin.

The current view has pinned recipient chips, fuzzy mention selection, explicit `@all`, replies and reply-all, escaped control characters, a latest-follow/unseen marker, and full-body detail scrolling. It keeps the latest 500 message rows in memory. `Home` does not page older history yet; it reports that limitation. The transport subscription resumes with its last cursor after reconnect, but this checkpoint did not force a reconnect during a live screen or test macOS. A separate backward-page API and peer synchronization remain open. Agent discovery text has not been installed globally.

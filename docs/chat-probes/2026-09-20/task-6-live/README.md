# Owned Codex idle and busy delivery gate

Candidate `59ad04a` was tested on 2026-09-20 against the existing Codex 0.153.4 app-server (PID 28534) through a 0.154.0 frontend. The test used only `/tmp/lam-chat-task6-native.KlGYDF/idle-case-BPY14n`: a private Chat database, a production release binary (SHA-256 `4d902aafd9f59fe3204be797c896735c2c8777209330c1e71c455f8af692c443`), and five project hooks (SHA-256 `5919305a1ae4b264f2ac0dd355922ee99b586112ffc0b4ca067c96db2b7a2d81`). The hooks were trusted through the normal Codex review UI. No bypass, shared daemon restart, global hook or native permission change was used. The ordinary `chat serve --foreground` path remained disabled; this test used its explicit `--native-bindings` gate switch.

The receiver's trusted SessionStart created Codex thread `01a0bfc7-84d3-73d0-92c2-0dc89aca740b`, incarnation `e323d8a5-7b35-4897-82e7-fe257f61b30e`. Its first turn completed with `READY`, and the Stop hook persisted idle observation epoch 2. A separately registered owned sender, thread `01a0bfc8-2474-7a81-8dae-f302957a9571`, sent `NATIVE_IDLE_QUEUE_TEST_59ad04a` through the actual `lam chat send --to MACHINE/INCARNATION --message TEXT` command. The first attempted invocation had an accidental extra `.` argument and failed Clap validation before any message write; the sender retried without it. Message `e56e3fa0-1347-434d-900e-4a2fe977411e` was persisted at 17:06:41.954 UTC. The service claimed it and recorded an exact native queue receipt at 17:06:42.139 UTC, attempt `cff2151e-490d-4a4e-b11c-2beebc2a421d`. A read-only query filtered to the receiver's exact native thread found a new user item carrying the fixed LAM wrapper and exact body, followed by the model's final response with that body and the sender Agent incarnation. This is positive idle wake and model-visible delivery for this version pair, separate from the native acceptance receipt.

For the busy boundary, the receiver was sent an ordinary owned native instruction to run one uninterrupted `sleep 60`. The exact-thread native history records `/bin/zsh -lc 'sleep 60'`, exit 0, duration 59,868 ms. While it was running, the same sender persisted `NATIVE_LONG_BUSY_TEST_59ad04a` as message `84b782e0-62af-48b7-9c99-2a9db63b74c4` at 17:08:56.408 UTC. Its receipt remained queued and no attempt existed during the tool. After completion, PostToolUse created attempt `f289e9df-9fb3-4f42-b157-d8c4e0cb30e6` at 17:09:42.880 UTC and finished it as **Unknown**, since hook stdout has no native acknowledgement. The receiver's same-turn final answer said `LONG_BUSY_DONE` and included the exact peer body and sender Agent incarnation. This proves non-interrupting inline exposure at that tool boundary; it does not turn Unknown into Accepted or prove every future model action.

A separate message named `NATIVE_BUSY_TEST_59ad04a` arrived after its shorter 20-second tool had already completed. It was correctly queued through the idle path and is **not** counted as busy evidence. No hostile-body, refusal, simultaneous-hook, restart, explicit SessionEnd/rotation, arrival-race or six-pair gate was run in this case. The existing [4096-byte hook and 100-empty-check case](../../2026-09-14/task-6-hook/README.md) remains the separate provenance/performance evidence. This test does not establish a standalone 0.154.0 backend or macOS behavior.

After the test, the sender frontend quit normally. The two other owned frontends were idle with all their exact-thread turns completed, then their exact PIDs and the owned daemon were terminated. Their frontend exits do not prove native SessionEnd. The active project hook file was moved to `hooks.disabled.json`, and only the two private test credential records and lock files were removed; the synthetic database was retained offline. The three owned processes were absent, hook diagnostics were absent, and shared backend PID 28534 remained alive. No production LAM installation was changed.
